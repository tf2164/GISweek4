class person {
    constructor(pName ,age, job){

        this.pName=pName;
        this.age=age;
        this.job;

    }
};

let person1 = new person(exercise);
console.log(person1);

    let fetchJob = () => person1.job
    let exercise = () => person1.exercise



//////////////////////////////////////////

class programmer {
    constructor(pName ,age, job,language) {

        this.pName=pName;
        this.age=age;
        this.job;
      this.language=language;
      this.busy=true;

    }
};

let programmer1 = new programmer(person1.pName, person1.age, person1.job), "English")
console.log(programer1);
    let fetchJob = () => person1.job
    console.log{`${person1} is a ${job}`}


let completeTask
let offerNewTask

if (busy=true){
    console.log(`${pName} would love to take on a new responsiblity`);
}
else{

    console.log(`${pName} can't accept any new tasks right now`);
}

let learnLanguage = () => new learnLanguage(arabic.language, mandarin.language, spanish.language);

let listLanguages= ()=> new listLanguages(python.language, javascript.language, css.language);
